import { useState } from 'react';
import { isEqual } from 'lodash';

export const useComplexFormStateComparison = (initialState: string, currentState: any) => {
  

  // const isDirty = !isEqual(initialStateRef, formState);

  // const resetForm = () => {
  //   setFormState(initialStateRef);
  // };

  // return {
  //   formState,
  //   setFormState,
  //   isDirty,
  //   resetForm,
  // };
};